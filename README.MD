此脚手架只是提供了最基本的功能，比如：
- 开发环境js热部署
- 生产环境代码压缩，提取公共包， runtime包，tree shaking, 抽取css文件

可以正常编写vue代码，以及sass文件。

此项目的目的是为了提供一个干净的脚手架，以便进行针对行开发时更容易修改。因此不适合对webpack及工程化没有经验的小白使用。

## mode
`mode`设置为`production`打包的时候会自动调用`UglifyJsPlugin`。效果是将文件压缩，并且不打包未使用的包和包未使用的方法。